import React from 'react';

function GetStartedSection() {
  return (
    <section className={'started'}>
      <h2 className={'started__title'}>GET STARTED</h2>
      <button className={'btn started__btn'}>NOW</button>
    </section>
  );
}

export default GetStartedSection;